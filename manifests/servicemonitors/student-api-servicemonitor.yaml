apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: student-api
  namespace: student-api
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: student-api
  endpoints:
  - port: http
    path: /metrics
    interval: 30s